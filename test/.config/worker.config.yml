trace:
  opentracing:
    jaeger:
      host: localhost:6831
transport:
  http:
    port: 8081
    user_agent: "Konfig/1.0 http/1.0"
    retry:
      timeout: 2s
      attempts: 3
    rate_limit:
      every: 1s
      burst: 10
      ttl: 24h
