trace:
  opentracing:
    kind: jaeger
    host: localhost:6831
transport:
  grpc:
    user_agent: "Konfig-client/1.0 grpc/1.0"
    retry:
      timeout: 2s
      attempts: 3
client:
  host: invalid_host
  timeout: 1s
  application: test
  version: v1.6.0
  environment: staging
  command: server
